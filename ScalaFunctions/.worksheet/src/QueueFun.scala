import scala.collection.immutable.Queue

object QueueFun extends Queue {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(99); 
  val empty=new Queue[Int];System.out.println("""empty  : <error> = """ + $show(empty ))}
}
